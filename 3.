#include <stdio.h>
#include <math.h>	

int main()
	{
		int a,b;
		printf("Unesite dva broja: \n");
		scanf("%d%d",&a,&b);		
		if(a>b) printf("%d is the maximum and %d is minimum number.",a,b);
		else printf("%d is the maximum and %d is the minimum number.",b,a);
	}	
